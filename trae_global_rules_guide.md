# Trae 全局规则维护指南

## 1. 全局规则体系架构

### 1.1 双层次规范结构

| 层次 | 文件位置 | 作用 | 维护方式 |
|------|----------|------|----------|
| **核心全局规则** | `d:\AICoding\ai-coding-standards\global_rules.md` | 定义 AI 身份、路由规则、安全底线 | 直接编辑该文件 |
| **具体实现规范** | `d:\AICoding\ai-coding-standards\prompts\style_guide.md` | 详细的编码规范和技术栈规则 | 基于全局规则扩展 |
| **提示词库** | `d:\AICoding\ai-coding-standards\prompts\` | 可复用的 AI 提示词模板 | 按需添加和更新 |
| **技能库** | `d:\AICoding\ai-coding-standards\skills\` | 跨项目通用能力扩展 | 模块化管理，独立更新 |

### 1.2 规范引用关系

```
global_rules.md (核心原则) ← style_guide.md (具体实现) ← 项目 context.md (项目状态)
```

## 2. 全局规则更新方法

### 2.1 直接更新核心规则

**文件**：`d:\AICoding\ai-coding-standards\global_rules.md`

**更新步骤**：
1. 使用文本编辑器打开该文件
2. 修改或添加规则内容
3. 更新文件头部的版本信息
4. 保存文件

**版本管理**：
- 主版本变更：核心架构或原则变更（如 v1.0 → v2.0）
- 次版本变更：规范细化、新增内容（如 v1.0 → v1.1）

### 2.2 更新具体实现规范

**文件**：`d:\AICoding\ai-coding-standards\prompts\style_guide.md`

**更新原则**：
- 必须遵循 `global_rules.md` 中的核心原则
- 专注于具体的编码规范和技术细节
- 避免与全局规则重复

**更新步骤**：
1. 参考 `global_rules.md` 中的最新原则
2. 修改或添加具体规范
3. 保持与全局规则的一致性
4. 保存文件

### 2.3 添加新的提示词

**目录**：`d:\AICoding\ai-coding-standards\prompts\`

**命名规范**：
- 使用小写字母和下划线
- 描述清晰的功能（如 `code_review.md`, `init_project.md`）

**内容要求**：
- 遵循 Markdown 格式
- 包含明确的使用场景
- 提供示例用法

### 2.4 管理技能库

**目录**：`d:\AICoding\ai-coding-standards\skills\`

**添加新技能**：
1. 创建新的技能目录（如 `new_skill/`）
2. 在目录中添加 `SKILL.md` 定义文件
3. 包含技能描述、安装说明、使用方法
4. 添加必要的脚本和依赖

**更新现有技能**：
1. 进入对应技能目录
2. 更新 `SKILL.md` 和相关脚本
3. 测试技能功能
4. 更新版本信息

## 3. 规范验证与生效

### 3.1 验证规范一致性

**检查内容**：
- 全局规则与具体规范的一致性
- 规范之间的引用关系
- 版本信息的准确性

**验证方法**：
```bash
# 在项目中测试规范应用
claude -p "请遵循 global_rules.md 和 style_guide.md 规范，创建一个简单的 Python 脚本"
```

### 3.2 规范生效机制

- **核心全局规则**：AI 启动时自动读取
- **具体规范**：按需引用（如项目初始化时引用 `init_project.md`）
- **提示词**：直接调用或通过斜杠命令使用
- **技能**：复制到项目 `.agent/skills` 目录后生效

## 4. 规范维护最佳实践

### 4.1 保持规范简洁

- 核心全局规则：只包含最基本的原则和路由
- 具体规范：专注于可执行的具体规则
- 避免冗余内容：删除过时或重复的规范

### 4.2 定期更新

- 每月检查一次规范的相关性
- 新项目启动前验证规范有效性
- 技术栈更新时同步更新相关规范

### 4.3 文档化变更

**更新 `工作日志.md`**：
```markdown
## 2026-01-20 全局规则体系更新
- [feat] 创建了 `global_rules.md`，建立双层次规范架构
- [refactor] 明确了全局规则与具体规范的职责划分
- [docs] 添加了规范维护指南
```

### 4.4 测试驱动规范

- 在实际项目中测试新规范
- 收集反馈，持续优化
- 记录规范应用中的问题和解决方案

## 5. 常见维护场景

### 5.1 添加新的技术栈规范

1. 更新 `global_rules.md`：添加技术栈支持的原则
2. 更新 `style_guide.md`：添加具体的技术栈规范
3. 创建相关提示词：如 `new_tech_stack_init.md`
4. 添加技能支持：如 `new_tech_stack_skill/`

### 5.2 修改交互原则

1. 更新 `global_rules.md`：修改交互原则部分
2. 验证新原则的可行性
3. 更新相关提示词
4. 文档化变更

### 5.3 修复规范冲突

1. 识别冲突点
2. 分析冲突原因
3. 调整规范内容
4. 验证修复效果
5. 更新相关文件

## 6. 全局规则使用指南

### 6.1 AI 启动时自动加载

AI 启动时会自动读取 `global_rules.md`，获取核心原则和路由规则。

### 6.2 手动引用规范

**在对话中引用**：
```
请遵循 global_rules.md 和 style_guide.md 规范，完成以下任务：
```

**在提示词中引用**：
```markdown
# 代码审查提示词
请按照以下规范审查代码：
1. 核心原则：参考 global_rules.md
2. 编码规范：参考 style_guide.md
3. 安全规则：参考 global_rules.md#5-安全底线
```

### 6.3 项目初始化引用

```bash
# 项目初始化时自动应用规范
claude -p "请按照 init_project.md 规范初始化项目"
```

## 7. 规范体系的优势

- **配置即代码**：所有规范以代码形式存储，便于版本控制
- **双层次架构**：核心原则与具体实现分离，便于维护和扩展
- **按需引用**：避免一次性加载过多内容，提高效率
- **跨项目复用**：规范集中管理，修改一处，所有项目受益
- **AI 原生设计**：Markdown 格式，便于 AI 直接读取和理解

## 8. 未来扩展方向

- **自动化验证工具**：开发工具自动检查规范执行情况
- **规范版本管理系统**：实现规范的版本控制和回滚
- **智能规范推荐**：根据项目类型自动推荐适用规范
- **规范使用统计**：分析规范的使用情况，优化热点规范

---

**注意**：由于编辑权限限制，无法直接修改 `style_guide.md` 添加对 `global_rules.md` 的引用。请手动更新该文件，在文件开头添加：

```markdown
# 全局代码风格与行为规范 (Style Guide)

此文件是 Trae 全局规则体系的具体实现，基于 `../global_rules.md` 制定，定义了所有 AI 编程任务必须严格遵守的详细规范。
```

这样可以确保规范体系的完整性和一致性。